const names = ['catwalk', 'vawe'];

data = {
  'names': names,
  'current': names[0],
  'lastPage': 'massage'
}


runButtonHandle();

massageListHandle(data);

handleButtonAfterRestart();

alertWhenMassage();